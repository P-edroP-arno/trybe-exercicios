// O laço for/of, retorna os elementos dentro de uma array, separadamente. Podemos somar a cada elemento um 'valor'.

// Exercício 1

let names = ['João', 'Maria', 'Antônio', 'Margarida'];

for (let nomes of names) {
    console.log(nomes);
}